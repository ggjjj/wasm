{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Rust",
            "type": "lldb",
            "request": "launch",
            "program": "./host/target/debug/host", // Change this to your compiled binary name
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}
